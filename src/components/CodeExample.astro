---
export interface Props {
    code: string;
    language?: string;
    variant?: 'neutral' | 'before' | 'after';
}

const {
    code,
    language = 'css',
    variant = 'neutral'
} = Astro.props;

const isPlainText = language === 'text';
const langLabel = isPlainText ? 'Diagram' : `${language.toUpperCase()} code example`;

const variantLabels = {
    neutral: langLabel,
    before: `Before, ${langLabel.toLowerCase()}`,
    after: `After, ${langLabel.toLowerCase()}`
};

const label = variantLabels[variant];
const languageClass = `language-${language}`;
/* `code` must stay plain text: Prism reads textContent and injects .token spans at runtime. */
---

<figure
    class:list={['code-example', `code-example--${variant}`]}
    data-code-example
    data-language={language}
    data-variant={variant}
>
    <figcaption>
        <span>{label}</span>

        <button class="copy" type="button" data-copy-code>
            Copy
        </button>
    </figcaption>

    <pre class={languageClass}><code class={languageClass}>{code}</code></pre>

    <p class="copy-status" data-copy-status aria-live="polite"></p>
</figure>
